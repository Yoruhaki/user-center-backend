from enum import Enum

USER_LOGIN_STATE = "userLoginState"

# ---- 权限 -----
DEFAULT_ROLE = 0

ADMIN_ROLE = 1

class UserConstants(Enum):
    USER_LOGIN_STATE = "userLoginState"

    DEFAULT_ROLE = 0
    ADMIN_ROLE = 1